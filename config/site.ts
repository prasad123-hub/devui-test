import { SiteConfig } from '@/types'

export const siteConfig: SiteConfig = {
  name: 'DevUI',
  description: 'An open source tailwind css component library.',
  url: 'https://devui.io',
  ogImage: '',
}
